import Foundation

// Placeholder to satisfy existing references after removing Core Data.
// The original Core Data stack has been removed in favor of Firebase.
final class CoreDataStack {
    static let shared = CoreDataStack()
    private init() {}
}
